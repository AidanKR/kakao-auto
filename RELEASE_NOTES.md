## 사진 백업의 한계를 밝히고, 대화방 진단 추가

설치 한 번, 그다음은 Enter 한 번(또는 매일 새벽 02:00 자동). **`KakaoAuto-Setup.exe`** 받아 실행하면 끝입니다.

### 이번에 고친 것 (v1.2.4)

- **사진이 하나도 백업되지 않는 이유를 밝혔습니다.** 카카오톡은 받은 이미지를 `.cng` 로 **암호화**해 캐시에 둡니다(실측: 같은 폴더 `.cng` 5개의 앞 8바이트가 전부 다름 — 평문 PNG라면 모두 `89 50 4E 47`로 시작). 복사해도 열리지 않습니다. 카카오톡에서 직접 저장한 파일만 백업할 수 있습니다.
- **잘못된 안내를 고쳤습니다.** 전에는 "받은 파일 저장 위치를 `media_dirs`에 지정하세요"라고 했는데, 지정해도 달라지지 않습니다. 이제 실제 이유와 선택지를 알려줍니다. 이 단계가 필요 없으면 `nightly_media: false`.
- **대화방 진단(메뉴 `15`)** — 대화방 창의 구조를 `room_tree.txt` 로 뜨고, '사진/동영상' 버튼 좌표를 보정해 저장합니다. 캐시를 뜯는 대신 카카오톡 자신에게 사진을 저장시키는 방식을 준비하는 단계입니다.

### 앞선 v1.2.2 · v1.2.3
목록을 아래로만 내리고 맨 위로 되돌리지 않아 매 회차가 바닥 몇 방만 돌던 문제, 그리고 목록 끝 판정이 '새 방 없음'이라 중복 구간에서 조기 종료되던 문제를 고쳤습니다. 새벽 회차 로그(`logs/nightly-*.log`)와 결과 요약, 수집 실패 시 종료코드 2도 들어갔습니다.

### 라이선스
**Apache License 2.0** ([LICENSE](LICENSE) · [NOTICE](NOTICE)). 상업적 사용·수정·재배포 무료, 대신 재배포 시 NOTICE의 원저작자 표시를 포함해야 합니다.

> 카카오톡 클라이언트 자동화는 이용약관 위반이며 계정 제재 위험이 있습니다. 본인 계정·본인 참여 대화·적법한 목적에만. 수집물의 개인정보 처리 책임은 사용자에게 있습니다.

전체 변경 이력: [CHANGELOG.md](https://github.com/AidanKR/kakao-auto/blob/main/CHANGELOG.md)
